package com.jasmine.Other.基础数学知识.算法;



public class 对数 {
    /**
        对数（logarithm），符号log
     */


    /*
        如果N = a ^ x (a > 0 ,a != 1)，即a的x次方等于N（a>0，且a≠1），那么数x叫做以a为底N的对数（logarithm）
      * 记作 x = logaN
        a：叫做对数的底数
        N：叫做真数
        x：叫做“以a为底N的对数”。

    1.特别地，我们称以10为底的对数叫做常用对数（common logarithm），并记为lg。
    2.称以无理数e（e=2.71828...）为底的对数称为自然对数（natural logarithm），并记为ln。
    3.零没有对数。
    4.在实数范围内，负数无对数。在复数范围内，负数是有对数的。



    lgN:
        以10为底的常用对数
        lgN = (x = log10N) = (N = 10^x)

    log2


    lnN
        以无理数e为底的自然对数 : lnN

     */
}
